[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# ✅ تحويل طلبات الـ API إلى سيرفر Render مباشرة
[[redirects]]
  from = "/api/*"
  to = "https://nasab-tree.onrender.com/api/:splat"
  status = 200
  force = true

# ✅ إصلاح مشكلة تحديث الصفحات في React
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200